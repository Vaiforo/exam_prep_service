# Сервис подготовки к экзамену по численным методам

Готовый Docker-сервис для подготовки к экзамену на основе предоставленных HTML-файлов с вопросами. В проект уже встроен извлечённый банк `questions.json`: 1000 основных вопросов + 20 вопросов образца.

## Возможности

- SQLite + SQLAlchemy ORM.
- Автоматический импорт вопросов при первом запуске.
- REST API на FastAPI.
- Адаптивная генерация тестов по статистике пользователя.
- Экзамен на 20 вопросов.
- Подготовка по темам.
- Подготовка по уровню готовности: 30%, 50%, 70%, 90%, 100%.
- Подготовка по сложности: лёгкие, средние, сложные.
- Повторение ошибок.
- Хранение истории прохождений, правильных/неправильных ответов, прогресса по темам и сложности.
- Экспорт/импорт прогресса в JSON для переноса на другую машину.
- После ответа показываются правильный ответ, объяснение, теория и готовый промпт для нейросети.
- Фронтенд без Node-сборки: обычный HTML/CSS/JS, отдаётся тем же FastAPI-сервисом.

## Запуск одной командой

```bash
docker compose up --build
```

После запуска открыть:

```text
http://localhost:8000
```

API-документация:

```text
http://localhost:8000/docs
```

## Структура проекта

```text
exam_prep_service/
├── app/
│   ├── app_data/questions.json      # структурированный банк вопросов
│   ├── static/                      # frontend
│   ├── database.py                  # подключение БД
│   ├── importer.py                  # импорт вопросов из JSON
│   ├── main.py                      # REST API + static frontend
│   ├── models.py                    # ORM-модели
│   └── services.py                  # логика тестов, статистики, экспорта
├── scripts/extract_questions.py     # парсер HTML-файлов
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md
```

## Данные

При первом запуске сервис создаёт SQLite-базу и импортирует данные из:

```text
app/app_data/questions.json
```

База хранится в Docker volume `exam_prep_sqlite`. Чтобы пересоздать базу с нуля:

```bash
docker compose down -v
docker compose up --build
```

## REST API

Основные endpoints:

| Метод | URL | Назначение |
|---|---|---|
| GET | `/api/health` | проверка сервиса |
| GET | `/api/topics` | список тем |
| GET | `/api/questions` | просмотр вопросов |
| POST | `/api/tests/start` | создать тестовую сессию |
| GET | `/api/sessions/{id}` | получить сессию |
| POST | `/api/sessions/{id}/answer` | ответить на вопрос |
| POST | `/api/sessions/{id}/finish` | завершить сессию |
| GET | `/api/stats` | статистика пользователя |
| GET | `/api/errors` | список ошибок |
| GET | `/api/export` | экспорт прогресса |
| POST | `/api/import/progress` | импорт прогресса |
| POST | `/api/import/questions` | импорт нового JSON-банка вопросов |

## Пример создания экзамена

```bash
curl -X POST http://localhost:8000/api/tests/start \
  -H "Content-Type: application/json" \
  -d '{"mode":"exam","count":20}'
```

## Пример теста по уровню готовности

```bash
curl -X POST http://localhost:8000/api/tests/start \
  -H "Content-Type: application/json" \
  -d '{"mode":"readiness","readiness_level":70,"count":20}'
```

## Импорт новых вопросов

Новый банк должен быть в формате `questions.json`, совместимом с текущей структурой:

```bash
curl -X POST "http://localhost:8000/api/import/questions?force=true" \
  -F "file=@questions.json"
```

## Расширяемость

Архитектура разделена так, чтобы можно было:

- добавить новые экзамены через JSON;
- заменить SQLite на PostgreSQL через `DATABASE_URL` без переписывания моделей;
- добавить авторизацию и несколько пользователей;
- подключить LLM для генерации расширенных объяснений;
- хранить векторные эмбеддинги вопросов и делать умный подбор похожих задач.

Для PostgreSQL достаточно заменить переменную окружения, например:

```text
DATABASE_URL=postgresql+psycopg://user:password@postgres:5432/exam_prep
```

И добавить соответствующий драйвер в `requirements.txt`.

## Замечание по качеству вопросов

В `questions.json` применены базовые исправления, найденные при аудите исходных HTML-файлов: исправлены неоднозначные варианты, часть формул и пояснений, в том числе коэффициент погрешности составной формулы Симпсона для обычного шага `h`.

## Примечания к версии v14

В этой версии дополнительно перепроверены старт на чистой SQLite-базе, авторизация, пользовательские тесты, импорт/экспорт прогресса и админ-панель.

Исправлено:

- удалён дублирующий индекс SQLite, из-за которого сервис мог падать при первом старте на чистой базе;
- импорт прогресса теперь работает как полное восстановление состояния пользователя: текущие сессии, ответы и ошибки пользователя предварительно очищаются, затем загружается состояние из JSON-файла;
- исправлен текст подтверждения импорта прогресса в интерфейсе;
- проверено, что `/docs`, `/redoc` и `/openapi.json` не доступны;
- проверено, что обычный пользовательский токен не подходит для `/api/admin/*`.

Для боевого сервера обязательно задайте `.env` с `ADMIN_USERNAME`, `ADMIN_PASSWORD` и ограниченным `CORS_ORIGINS`.

## Примечания к версии v15

Добавлен статус пользователей в админ-панели:

- `Онлайн` — пользователь был активен в последние `ONLINE_TIMEOUT_SECONDS` секунд;
- `Оффлайн` — активности нет или пользователь вышел из аккаунта;
- по умолчанию `ONLINE_TIMEOUT_SECONDS=300`.

Статус обновляется при пользовательских API-запросах и через heartbeat интерфейса, пока вкладка сервиса открыта.

## Примечания к версии v16

Добавлена иконка сайта и переключатель темы оформления:

- favicon `32x32`;
- app icon `192x192` и `512x512`;
- apple touch icon;
- `site.webmanifest`;
- логотип в шапке основного интерфейса и админ-панели;
- переключатель светлой/тёмной темы в основном интерфейсе и админке;
- выбранная тема сохраняется в `localStorage` и применяется при следующем открытии.

## Примечания к версии v17

Исправлена ошибка админ-панели: файл `admin_static/admin.js` был восстановлен как отдельный админский скрипт и больше не пересекается с основным `app/static/app.js`.

Дополнительно добавлены проверки:

- админский JS содержит `/api/admin/login`, а основной JS — только пользовательскую авторизацию;
- `/admin` отдаёт страницу админки;
- `/admin-static/admin.js` отдаёт именно админский скрипт;
- ссылки на DOM-элементы в основном интерфейсе и админке проверены;
- интеграционные тесты backend и статической раздачи проходят успешно.

## Примечания к версии v18

Исправлена тёмная тема:

- усилен контраст вторичных кнопок, навигации, кнопок сложности и карточек режима;
- теория, краткая теория, теория в карточках и блоки формул в тёмной теме теперь имеют тёмный фон и светлый текст;
- правильный ответ в тёмной теме выделяется ярким зелёным;
- ошибка в тёмной теме выделяется ярким красным;
- в админ-панели даты создания и последней активности пользователей отображаются в часовом поясе Омска (`Asia/Omsk`).

## Примечания к версии v19

В тёмной теме уменьшена яркость основных синих кнопок: основной цвет кнопок установлен в `#1d4ed8`, чтобы белый текст читался комфортнее.

## Примечания к версии v20

Добавлена защита от устаревшего кеша интерфейса после обновления:

- CSS/JS подключаются с версией `?v=APP_VERSION`;
- `APP_VERSION` можно задавать в `.env`;
- для HTML/CSS/JS выставляются заголовки `Cache-Control: no-cache, no-store, must-revalidate`;
- `/` и `/admin` отдаются через backend с подстановкой актуальной версии.

Пример `.env`:

```text
APP_VERSION=20
```

При следующем деплое достаточно поменять `APP_VERSION=21` и перезапустить контейнер.

## Примечания к версии v21

В тёмной теме возвращено различие между основными и вторичными кнопками:

- основные действия остаются синими;
- `secondary`-кнопки вроде «Предыдущий», «Дальше», «На главную», переключатель темы и похожие стали тёмно-серыми;
- светлая тема не изменялась.

## Примечания к версии v22

Проведена полная чистка теории:

- переписаны и нормализованы теоретические блоки для всех 44 тем;
- краткая теория у всех 1000 основных вопросов и 20 вопросов образца синхронизирована с исправленной теорией темы;
- пустые блоки теории отсутствуют;
- убраны Markdown/code delimiters, из-за которых формулы могли отображаться как код;
- формулы приведены к MathJax-формату `\(...\)` и `\[...\]`;
- рендер теории дополнительно схлопывает многострочные display-формулы, чтобы они не распадались на отдельные строки.


## v23

- Расширена теория по всем 44 темам: добавлены наглядные примеры и памятки.
- Краткая теория у всех вопросов синхронизирована с расширенной теорией тем.
- Онлайн-статус пользователя обновляется не только по таймеру, но и при кликах, тапах и нажатиях клавиш на сайте.
- Добавлен endpoint `/api/auth/ping` для безопасного обновления активности авторизованного пользователя.


## v24

- Теория расширена мини-примерами к ключевым определениям и коротким формулировкам.
- Примеры добавлены не только в конец темы, но и перед общим примером как быстрые пояснения к важным фразам.
- Краткая теория всех вопросов синхронизирована с расширенной теорией.
- APP_VERSION обновлён до 24 для обновления кеша статики.


## v25

- Теория перестроена: мини-примеры встроены прямо в ход объяснения, рядом с соответствующими определениями.
- Добавлены контрпримеры и контрастные ситуации: когда метод работает хорошо, а когда плохо.
- Первая тема переписана вручную в более подробном формате с примерами к каждому ключевому понятию.
- Краткая теория всех вопросов синхронизирована с новой структурой.
- APP_VERSION обновлён до 25.


## v26

- Теория расширена до формата самостоятельного учебного конспекта.
- Для каждой темы добавлены интуиция, порядок применения, типичные ошибки и мини-задача с ответом.
- Теория всех вопросов синхронизирована с обновлёнными темами.
- APP_VERSION обновлён до 26.


## v27

- Улучшено отображение теории: примеры, контрпримеры, интуиция, шаги и ошибки выводятся как отдельные читаемые карточки.
- Ответы в мини-задачах скрыты в раскрывающемся блоке «Показать ответ».
- Добавлены стили для светлой и тёмной темы.
- APP_VERSION обновлён до 27.


## v28

- Все 44 мини-задачи переписаны в явном формате: вопрос + скрытый ответ.
- Исправлены мини-задачи, которые раньше выглядели как утверждения без вопроса.
- Рендер мини-задач стал устойчивее: если ответ отсутствует в будущих данных, интерфейс показывает понятную заглушку.
- APP_VERSION обновлён до 28.


## v29

- Проверены все текстовые поля банка вопросов на MathJax-разметку.
- Исправлены raw LaTeX-матрицы в вариантах ответов: теперь они обёрнуты в `\(...\)` и отображаются как формулы.
- Убраны оставшиеся `$...$` из служебных метаданных.
- APP_VERSION обновлён до 29.


## v30

- Проведена полная проверка MathJax-разметки во всех текстовых полях банка вопросов.
- Raw LaTeX-матрицы в вариантах ответов обёрнуты в MathJax-разделители.
- В тёмной теме цвета карточек теории сделаны мягче и спокойнее.
- APP_VERSION обновлён до 30.


## v31 — исправление сломанной формулы обратной матрицы

- Исправлена формула в теме 20: теперь условие существования обратной матрицы отображается как `\(\det A\ne 0\)`.
- Проверены все теоретические блоки, вопросы, варианты и объяснения на сломанные LaTeX-команды.
- Сохранены мягкие цвета карточек теории в тёмной теме из v30.
- `APP_VERSION` обновлён до `31` для автоматического обновления CSS/JS у пользователей.


## v32 — жалобы на ошибки и фильтр онлайн-пользователей

- Добавлена кнопка «Пожаловаться на ошибку» в вопросах и теории.
- Жалоба открывает форму внизу страницы и автоматически прокручивает пользователя к ней.
- Жалобы сохраняются в базе и доступны только администратору в `/admin`.
- В админке видно отправителя, текст жалобы, связанный вопрос или тему и статус обработки.
- В админке можно менять статус обращения и удалять уже обработанные обращения.
- У пользователей есть кулдаун на отправку жалоб: `REPORT_COOLDOWN_SECONDS=60`.
- Добавлен фильтр пользователей: все / онлайн / оффлайн.
- `APP_VERSION` обновлён до `32`.


## v33 — простая и стандартная теория

- Добавлены два режима теории: «Простая» и «Стандартная».
- Стандартная теория — текущая подробная версия с формулами.
- Простая теория объясняет темы более человеческим языком, с минимумом сложных формул, но с обязательными терминами для ответа преподавателю.
- Переключатель доступен в разделе теории, в краткой теории после ответа и на карточках.
- `APP_VERSION` обновлён до `33`.


## v34 — простая теория с обязательными формулами

В простой теории сохранены ключевые расчётные формулы: они объясняются простыми словами и показывают, что именно нужно подставлять при решении задач.


## v35 — вкладки админки и метрики

Админ-панель разделена на горизонтальные вкладки: пользователи, жалобы на ошибки и метрики. Добавлен поиск пользователя по логину, метрики скорости ответов, загрузки страницы, ошибок, online-пользователей и активных тестов.


## v36 — повторение ошибок одной кнопкой

В разделе ошибок убраны повторяющиеся кнопки у каждой карточки. Добавлена одна верхняя кнопка «Повторить ошибки», запускающая тест только по вопросам, где пользователь ошибался.
